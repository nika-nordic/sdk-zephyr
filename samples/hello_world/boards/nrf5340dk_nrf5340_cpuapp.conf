CONFIG_UART_ASYNC_API=y

# Enable the clock-management framework and let the UARTE driver apply the
# instance clock states around peripheral activity.
CONFIG_CLOCK_MANAGEMENT=y
CONFIG_UART_NRFX_UARTE_CLOCK_MGMT_ON_ACT=y
